apiVersion: v1
kind: Service
metadata:
  name: fcg-games-service
  labels:
    app: fcg-games-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: fcg-games-service
